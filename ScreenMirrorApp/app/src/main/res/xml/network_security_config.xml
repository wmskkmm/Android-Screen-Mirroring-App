<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 允许局域网 cleartext 通信（UDP 流传输不受影响，但 NSD 发现需要） -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
